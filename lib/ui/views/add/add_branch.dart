import 'package:flutter/material.dart';

class AddBranch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Add Branch here"),
      ),
    );
  }
}
