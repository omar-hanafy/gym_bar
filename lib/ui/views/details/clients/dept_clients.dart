import 'package:flutter/material.dart';

class DeptEmployee extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Dept Employee Profile Here"),
      ),
    );
  }
}
